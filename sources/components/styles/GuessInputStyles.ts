import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        fontSize: 24,
        fontWeight: "bold",
        borderWidth: 2,
        borderColor: "#000",
        padding: 10,
        width: "35%",
        marginRight: 10,
      }
});